#include<stdio.h>
#include <math.h>  
int main()  
{  
    int N;  
  
  
    scanf("%d",&N);  
  
    int i,sum=0;  
    int x=1,y=1;  
    for(i=0;i<N-1;i++) x*=10;   
    for(i=0;i<N;i++) y*=10;  
      
    for(i=x;i<y;i++){  
        int m=i;  
        sum=0;  
        while(m>0)  
        {  
            int temp=1;  
            int count=N;  
            while(count--) temp*=(m%10);  
            sum+=temp;    
            m/=10;  
        }  
        if(sum==i) printf("%d\n",i);  
    }  
    return 0;  
}
